package com.mycompany.java_practice_project.OOP.FinalKeywordTw;
//we can not extend the class when the final keyword before the class
public /*final*/ class University {
    //we can not override the method when the final keword before the method
    final void display(){
        System.out.print("DIU");
    }
}
