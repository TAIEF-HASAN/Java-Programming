package com.mycompany.java_practice_project.Package;

//import java.util.*; We can call a package like this, (*) means all
import java.util.Scanner; // we can call a package like this. it means we need here Scanner class for input. So we not need to import all class form java.util package
public class Dog {
    //We can import package like this
    //java.util.Scanner n = new java.util.Scanner(System.in);
    Scanner n = new Scanner(System.in);

}
